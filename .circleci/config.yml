version: 2.1

orbs:
  node: circleci/node@4.0.0

workflows:
  app-tests:
    jobs:
      - test


jobs:
  test:
      docker:
        - image: cimg/node:15.11.0
      steps:
        - checkout
        - run: node --version
